# -*- coding: utf-8 -*-
"""时间基准彻查: 3条链 (官谱/RPE转换/collect_all_notes)"""
import os, sys, io, json, numpy as np
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')
_ROOT = os.path.abspath(os.path.join(os.path.dirname(os.path.abspath(__file__)), '..'))
sys.path.insert(0, _ROOT)

print('=== 1. RPE [m,b,d] 语义 ===')
print('RPE: startTime=[measure, beat, division]')
print('  measure=小节, beat=拍内位置, division=每拍等分数')
print('  例: [1,0,1] = 第2小节开始 = 第4拍(0-indexed) = 4拍')
print('  例: [0,1,2] = 第1小节第2个半拍 = 0.5拍')
print('\n=== 2. predict_rpe.py 转换 ===')
print('  start_time = (m*4.0 + b*(4.0/d)) * 8.0')
print('  → (小节×4拍 + 拍位置) × 8')
print('  → 如果 1拍=8tick, 则 [1,0,1] → (4+0)*8=32tick')
print('  → 官谱 1拍=32tick! 所以 RPE→standard 后 1拍=8tick 是错的?')
print('\n=== 3. _normalize_time_st (feature_extractor) ===')
print('  beat = st[0] + st[1]/st[2]')
print('  → [1,0,1] = 1拍 (没有×4!)')
print('  → ticks = beat*32 = 32tick')
print('  → 官谱 1拍=32tick ✓')
print('\n=== 矛盾 ===')
print('predict_rpe: [1,0,1] → 32tick (1拍=8tick? [1,0,1]按m*4=4拍*8=32)')
print('normalize:  [1,0,1] → 32tick (1拍=32tick? beat=1拍*32)')
print('两者tick值相同(32), 但语义不同!')
print('predict_rpe认为[1,0,1]=4拍, normalize认为=1拍')
print('\n=== 验证: 用BPM事件对齐 ===')
print('Melodiniq BPM: 193@[0,0,1] → 196@[256,0,1]')
print('predict_rpe: [256,0,1] → (256*4)*8 = 8192tick')
print('normalize:  [256,0,1] → (256)*32 = 8192tick')
print('两者都=8192!')
print('\n但拍数: predict_rpe认为256小节=1024拍, normalize认为256拍')
print('BPM时间线(_parse_bpm_timeline): start_beat = 256 + 0/1 = 256拍')
print('→ collect_all_notes 里 _resolve_bpm_at_beat(beat_time=ticks/32)')
print('  音符tick=8192 → beat_time=8192/32=256拍')
print('  BPM时间线start_beat=256 → 匹配196bpm ✓ (巧合对齐!)')
print('DONE')